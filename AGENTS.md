# PPP 源码学习标注规则

以下规则适用于在本仓库中陪同用户学习和标注 PPP 源码的任务。

1. 每次只讲解当前执行顺序中的一小段代码；必须等用户明确表示看懂后，才能继续下一段。
2. 中文解释应写在对应代码行的右侧，优先使用完整闭合的行尾注释 `/* ... */`，不要把多行说明堆在代码块前面。
3. 含义相近且简单的声明或语句可以一次讲解多行；关键公式、状态更新和函数调用必须放慢速度，逐行讲解。
4. 注释使用面向初学者的中文，先解释变量或语句“用来做什么”，再解释必要的语法和数学含义。
5. 学习标注只能增加注释或进行不改变语义的排版拆行，不得修改 PPP 算法、参数、计算顺序或程序行为。
6. 修改前必须确认当前 Git 分支是 `ppp-study`；不得把学习注释写入 `main` 分支。
7. 应把补丁锚定在当前讲解的函数上下文中，不能只按一条可能重复出现的代码进行全文件匹配。
8. 每次修改后必须检查注释实际出现的函数和行号，并运行 `git diff --check`；不得只凭补丁成功提示判断标注位置正确。
9. 所有 `/*` 必须在同一条注释中正确配对 `*/`。涉及函数签名、括号或代码排版变化时，应进行实际编译验证。
10. 不修改、删除或提交用户已有的无关文件和改动，也不自动执行 `git add` 或 `git commit`。

## 手机 PPP 研究与实验规则

以下规则适用于本仓库的手机 PPP 研究、代码修改和对照实验；源码学习标注
仍需同时遵守上面的专用规则。

1. 开始工作前必须阅读 `DEEPSEEK_HANDOFF.md`、`doc/手机PPP研究进展.md`
   和 `doc/手机PPP定权对照实验.md`，并检查 `git branch --show-current`、
   `git status --short` 和最近提交。
2. `main`、`ppp-study`、`ppp-cn0-weight` 和已经完成的实验分支不得直接叠加
   新算法。每个研究问题新建独立分支；一个提交和一轮对照只改变一个主要变量。
3. 不得修改原始 `gnss_log.txt`、RINEX、精密产品、广播星历或
   `ground_truth.csv`。生成结果必须使用新名称，不得覆盖 SPP/A/B/C/D 基线。
4. 修改算法前先写明假设、对照组、实验组、固定参数、开发数据和判定指标。
   结果不支持假设时如实保留负结果，不通过事后换指标或删除失败设备包装结论。
5. 所有方案使用同一评价脚本和 GPST−18 s 的时间对齐，同时报告直接解成功率、
   Q=6 数量、P50、P95、挑战分数、最大值及完整时间轴缺失处理。不能只比较
   各方案幸存历元。
6. SPP 和 A 高度角 PPP 是绝对基线；B/C 是已有随机模型对照。新方案必须在
   相同轨迹、设备、产品和配置基础上比较，不得为单台手机暗改单独参数。
7. 开发集可用于提出和调试模型；最终验证集在参数冻结前不得用于挑参。若尚未
   正式划分开发集和验证集，结论只能称为探索性或开发集结果。
8. 每次源码修改后运行 `git diff --check`，检查补丁所在函数和准确行号，并至少
   构建 `rnx2rtkp` 目标。全量构建的 LAPACK/BLAS 链接错误要与源码编译错误区分。
9. 构建后必须确认运行程序加载的是新 DLL：将 `lib/librtklib.dll` 同步到
   `bin/librtklib.dll`，核对二者 SHA-256，再运行实验。结果异常相同应先排查
   旧 DLL、错误分支、错误配置和错误输入路径。
10. 不把 RINEX `LLI=2` 等同于真实整周周跳；RINEX 可能丢失 Android ADR 的
    `RESET` 信息。周跳/钟跳结论必须结合 ADR、Doppler、GF/MW、残差和时间连续性。
11. 不得把当前 `PPP_LLI_HALFC_AS_SLIP=0` 的 D 实验宣称为通用改进；已验证它
    主要有诊断价值，下一阶段误差模型实验必须恢复基线 LLI 行为并单独建分支。
12. 每完成一轮正式实验，及时更新 `doc/手机PPP研究进展.md` 的版本状态、方法、
    完整结果、限制、当前结论和下一步；路径、命令和指标必须可复现。
13. 未经用户明确授权，不执行 `git add`、`git commit`、`git push`、合并、删除
    分支或覆盖结果。保留用户已有的无关修改和未跟踪文件。
14. 面向用户解释时按初学者水平进行：简单重复操作可以一次给出多行命令；
    公式、状态估计、误差模型和关键代码改动必须分步解释并等待用户确认理解。

### 开发/验证数据划分（2026-09-03 固定，适用于 P2 及以后所有参数选择）

开发集（只能依据这三组数据提出、调试和选择模型/参数）：

1. `2021-07-14-US-MTV-1 / XiaomiMi8`
2. `2021-12-08-US-LAX-1 / XiaomiMi8`
3. `2021-07-14-US-MTV-1 / SamsungGalaxyS20Ultra`

内部留出集（不参与调参，只用于模型冻结后的检查；因六组结果此前已被查看过，
只能称为内部留出验证，不能宣称独立验证）：

1. `2021-07-01-US-MTV-1 / XiaomiMi8`：检查灾难性发散和鲁棒性；
2. `2021-12-08-US-LAX-1 / SamsungGalaxyS20Ultra`：跨场景、跨设备检查。

压力失败案例：

- `2021-12-08-US-LAX-1 / GooglePixel5`：当前传统 PPP 本身失败，不参与普通
  平均分数和参数选择；单独报告能否恢复、有效历元数及千米级相位残差是否改善。

约束：

- 开发阶段只能根据三组开发集选模型；内部留出集和 Pixel 压力案例不得用于
  反复调参，只能在参数冻结后各看一次作为检查。
- `varerr()` 公式和参数冻结后，必须再下载此前未分析过的新轨迹作为正式独立
  验证集，才允许把结论表述为独立验证结果。

